#ifndef BIBLIOTECA_H_INCLUDED
#define BIBLIOTECA_H_INCLUDED

/** \brief
 *
 * \param numeroA float es el primer operando.
 * \param numeroB float es el segundo operando.
 * \return float es el resultado de la suma entre ambos operadores.
 *
 */
float sumar(float numeroA, float numeroB);

/** \brief
 *
 * \param numeroA float es el primer operando.
 * \param numeroB float es el segundo operando.
 * \return float es el resultado de la resta entre ambos operadores.
 *
 */
float restar(float numeroA, float numeroB);

/** \brief
 *
 * \param numeroA float es el primer operando.
 * \param numeroB float es el segundo operando.
 * \return float es el resultado de la division entre ambos operadores.
 *
 */
float dividir(float numeroA, float numeroB);

/** \brief
 *
 * \param numeroA float es el primer operando.
 * \param numeroB float es el segundo operando.
 * \return float es el resultado de la multiplicacion entre ambos operadores.
 *
 */
float multiplicar(float numeroA, float numeroB);

/** \brief
 *
 * \param numeroA float es el primer operando.
 * \param numeroB float es el segundo operando.
 * \param float* es un puntero.
 * \param float* es un puntero.
 * \return float es el resultado del factorial de ambos operadores.
 *
 */
float factorial(float numeroA,float numeroB,float *,float *);

#endif // BIBLIOTECA_H_INCLUDED
